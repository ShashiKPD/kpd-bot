
const markEveryoneAbsent = async (startingDate, noOfMeals) => {
  // logic to mark everyone absent
};

const markUserAbsent = async (personID, startingDate, noOfMeals) => {
  // logic to mark a specific person absent
};


export {markEveryoneAbsent, markUserAbsent}; 